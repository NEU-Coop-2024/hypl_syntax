@top Program { expression* }

expression {
  Identifier |
  Name |
  Application { "(" expression* ")" }
}

@tokens {
  Identifier { $[a-z] $[a-zA-Z0-9_]* }
  Name { $[A-Z] $[a-zA-Z0-9_]* }
  Number { digit }
}

@detectDelim
